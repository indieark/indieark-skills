name: ppt-gen-pro
description: Mandatory presentation generation router. Trigger for PPT/PPTX/slides/decks, route among five L1 forms (image-first, web/HTML, design HTML, SVG/editable, academic-image) plus L2 post-process (video-html-ppt for web-html-ppt recording), introduce the L1 routes, recommend one, ask the user to choose, then delegate to the selected installed extension.
entrypoint: SKILL.md
